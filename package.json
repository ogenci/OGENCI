{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --if-present run typecheck",
    "dev": "PORT=3000 pnpm --filter @workspace/api-server dev & PORT=8080 BASE_PATH=/ pnpm --filter @workspace/open-design-clone dev",
    "dev:all": "pnpm --env-file=.env -r --parallel dev",
    "dev:app": "PORT=8080 BASE_PATH=/ pnpm --filter @workspace/open-design-clone dev",
    "dev:sandbox": "PORT=8081 BASE_PATH=/ pnpm --filter @workspace/mockup-sandbox dev",
    "dev:api": "PORT=3000 pnpm --filter @workspace/api-server dev"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
